export interface IDepartamento {
  id: string;
  departamento: string;
}

export interface IProvincia {
  id: string;
  provincia: string;
}

export interface IDistrito {
  id: string;
  distrito: string;
}
